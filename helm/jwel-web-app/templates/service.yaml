# Service for the application
apiVersion: v1
kind: Service
metadata:
  name: jwel-web-app
  labels:
    app: jwel-web-app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: jwel-web-app
  type: NodePort
